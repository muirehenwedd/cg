namespace Primitives3D;

public struct Camera
{
    public Camera(
        Point viewPoint,
        Vector viewDirection,
        float screenDistance,
        int screenWidth,
        int screenHeight,
        float pixelSize
    )
    {
        // todo
    }

    public Camera(
        Point viewPoint,
        Vector viewDirection,
        float screenDistance,
        float fieldOfView,
        int screenSize
    )
    {
        // todo
    }
}